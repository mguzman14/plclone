all:
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
    tailscale_key: "{{ lookup('env', 'TS_KEY') }}"

    flota_plc:
      red_industrial_1:
        subred: "192.168.2.0/24" # 👈 AÑADE ESTO
        dispositivos:
          - nombre: "plc-bombeo-entrada"
            ip_fija: "192.168.2.10"
          - nombre: "plc-filtracion"
            ip_fija: "192.168.2.20"

      red_industrial_2:
        subred: "192.168.3.0/24" # 👈 AÑADE ESTO
        dispositivos:
          - nombre: "plc-deposito"
            ip_fija: "192.168.3.10"
  
  hosts:
    etap:
      ansible_host: "{{ lookup('env', 'HOST1_IP') }}"
      ansible_user: "{{ lookup('env', 'HOST1_USR') }}"
      ansible_become_password: "{{ lookup('env', 'HOST1_PWD') }}"
      ansible_connection: local


   

  children:
    prod:
      hosts:
        etap:
    dev:
      hosts:
        etap: